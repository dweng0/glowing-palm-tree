import LadderWrapper from "./index";
describe("tests for the ladder wrapper", () => { 
    it("Should compile at runtime", () => expect(LadderWrapper).toBeDefined());
    it("Should call the reducer correcty", () => { 
        //setup
        //execute
        //verify
    })
})